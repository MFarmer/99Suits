<% if @items.count > 0 %>
  <% @items.each do |item| %>
    <%= render partial: "dashboard/thumbnail", :locals => { :item => item } %>
  <% end %>
<% else %>
  <p>There are no items to display at this time.</p>
<% end %>